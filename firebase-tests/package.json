{
  "private": true,
  "scripts": {"test": "mocha *.test.cjs --timeout 30000"},
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "mocha": "^11.0.0"
  }
}
